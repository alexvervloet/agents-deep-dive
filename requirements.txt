# Loads variables from a .env file (like OPENAI_API_KEY) into the environment.
python-dotenv>=1.0.0,<2

# rich renders the agent's step-by-step trace and the capstone's output.
rich>=13.0.0,<16

# jsonschema enforces tool input contracts locally before any function runs.
jsonschema>=4.0,<5

# --- Provider SDKs ---
# You only strictly need the one matching your PROVIDER (see .env). Both are
# light, and installing both lets you switch stacks with one env-var change.

# OpenAI: used when PROVIDER=openai (chat + function calling).
openai>=2.0,<3

# Anthropic: used when PROVIDER=claude (messages + tool use).
anthropic>=0.111.0,<1
